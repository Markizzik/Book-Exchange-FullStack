[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: fast isolated checks for pure logic
    integration: API and database integration checks
    e2e: end-to-end business flow checks
addopts = --strict-markers --cov=app --cov-report=term-missing --cov-fail-under=55
